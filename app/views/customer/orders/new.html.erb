<h2>注文情報入力</h2>
<%= form_with model: @order, url: oders_confirm_path(@order), method: :post, local: true do |f| %>
<h2>支払い方法</h2>
<%= f.radio_button :payment_method, :credit %>
<%= f.label 'クレジットカード' %>

<%= f.radio_button :payment_method, :bank %>
<%= f.label '銀行振込' %>

<h2>支払い方法</h2>
<%= f.radio_button :payment_method, :credit %>
<%= f.label 'クレジットカード' %>

<%= f.radio_button :payment_method, :bank %>
<%= f.label '銀行振込' %>